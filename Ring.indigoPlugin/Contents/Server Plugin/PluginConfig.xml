<?xml version="1.0"?>
<PluginConfig>
	<Field id="infoLabel" type="label">
		<Label>Ring Doorbell Plugin</Label>
	</Field>
	<Field type="textfield" id="UserID" tooltip="Ring Username" visibleBindingValue="true">
		<Label>Ring Username:</Label>
	</Field>
	<Field type="textfield" id="Password" secure="true" tooltip="Ring Password" visibleBindingValue="true">
		<Label>Ring Password:</Label>
	</Field>
	<Field type="textfield" id="DownloadFilePath" tooltip="Path and filename to download video ro" visibleBindingValue="true">
		<Label>Path and filename for downloading video with Download Video action:</Label>
	</Field>
	<Field type="textfield" id="maxRetry" tooltip="Max Retries before Failure" defaultValue="5">
		<Label>Max refresh retry attempts:</Label>
	</Field>
	<Field id="retryNote" type="label" fontSize="small" fontColor="darkgray">
        <Label>The plugin will never stop retrying on error if the retry attempts is set to zero. </Label>
    </Field>
	 <Field id="sep1" type="separator"/>
    <Field id="updateFrequency" type="textfield" defaultValue="24">
        <Label>Check for plugin update frequency (hours):</Label>
    </Field>
    <Field id="updateNote" type="label" fontSize="small" fontColor="darkgray">
        <Label>The plugin will not check for updates if the frequency is set to zero.</Label>
    </Field>
	<Field id="simpleSeparator6" type="separator"/>
	<Field type="checkbox" id="sendTelemetry"
		tooltip="Enables sending Telemetry to plugin developer." defaultValue="true">
		<Label>Enable Telemetry:</Label>
	</Field>
	<Field id="telemetryNote" type="label" fontSize="small" fontColor="darkgray">
        <Label>Telemetry will help the plugin developer catch errors and help improve it.</Label>
    </Field>
	<Field id="simpleSeparator5" type="separator"/>
	<Field type="checkbox" id="showDebugInLog"
		tooltip="This enables lots of debugging messages in the Indigo Event Log.">
		<Label>Enable debugging:</Label>
		<Description>(use carefully)</Description>
	</Field>
</PluginConfig>
